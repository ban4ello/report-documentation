# Calculations and Analysis - Client

Веб-приложение для расчетов и анализа данных, построенное на Vue 3.

## 🚀 Технологический стек

- **Vue 3** - Прогрессивный JavaScript фреймворк
- **Vite** - Быстрый инструмент сборки
- **PrimeVue** - UI библиотека компонентов
- **Vue Router** - Маршрутизация для SPA
- **Axios** - HTTP клиент для API запросов
- **Chart.js** - Библиотека для создания графиков
- **Tailwind CSS** - Utility-first CSS фреймворк
- **Sass** - CSS препроцессор

## 📋 Функциональность

### Основные модули:
- **Dashboard** - Главная панель с аналитикой и графиками
- **Calculations** - Управление расчетами
  - Просмотр списка расчетов
  - Создание новых расчетов
  - Детальный просмотр расчетов
- **UI Kit** - Демонстрация компонентов интерфейса
- **Аутентификация** - Система входа и управления доступом

### Компоненты UI:
- Формы и поля ввода
- Таблицы и списки
- Графики и диаграммы
- Модальные окна и оверлеи
- Меню и навигация
- Файловые компоненты
- Timeline и другие элементы

## 🛠 Установка и запуск

### Предварительные требования
- Node.js (версия 16 или выше)
- npm или yarn
- Работающий backend сервер (см. раздел "🔧 Конфигурация")
- Git

### Клонирование репозитория

Для получения исходного кода проекта выполните:

```bash
git clone https://github.com/ban4ello/report-documentation.git client
cd client
```

Эта команда:
- Клонирует репозиторий из GitHub в директорию `client`
- Если директория `client` уже существует, создаст её, перезаписав существующую
- После клонирования перейдите в директорию проекта для дальнейшей установки зависимостей

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### Сборка для продакшена
```bash
npm run build
```

### Предварительный просмотр сборки
```bash
npm run preview
```

### Линтинг кода
```bash
npm run lint
```

## 📁 Структура проекта

```
src/
├── assets/           # Статические ресурсы и стили
│   ├── demo/         # Демо стили и ресурсы
│   ├── layout/       # Стили макета
│   ├── styles.scss   # Основные стили
│   └── tailwind.css  # Tailwind CSS
├── components/       # Переиспользуемые компоненты
│   ├── custom-ui/    # Кастомные UI компоненты
│   └── ...           # Другие компоненты
├── layout/           # Компоненты макета
│   ├── composables/  # Vue композаблы
│   └── ...           # Компоненты макета
├── router/           # Конфигурация маршрутизации
├── service/          # API сервисы
├── views/            # Страницы приложения
│   ├── pages/        # Основные страницы
│   └── uikit/        # Демо UI компонентов
└── main.js          # Точка входа приложения
```

## 🎨 Дизайн и темизация

Приложение использует:
- **PrimeVue Aura Theme** - Современная тема по умолчанию
- **Tailwind CSS** - Для кастомных стилей
- **Темная тема** - Поддержка темного режима
- **Адаптивный дизайн** - Оптимизация для всех устройств

## 🔧 Конфигурация

### Переменные окружения (.env)

Для корректной работы приложения необходимо создать файл `.env` в корне проекта `client` и настроить следующие переменные:

```bash
# URL серверного API
VITE_API_HOST=http://localhost:8000

# Пример для продакшена:
# VITE_API_HOST=https://api.example.com
```

#### Важные заметки:
- **Префикс VITE_** - обязателен для всех переменных окружения, которые должны быть доступны в браузере (требование Vite)
- Файл `.env` уже добавлен в `.gitignore`, чтобы избежать коммита конфиденциальных данных
- Для разных окружений можно использовать:
  - `.env` - для локальной разработки
  - `.env.production` - для продакшена
  - `.env.development` - для разработки

#### Использование переменных окружения:

В коде приложения переменные доступны через `import.meta.env`:

```javascript
// Пример из src/service/config.js
baseURL: `${import.meta.env.VITE_API_HOST}/api/`
```

### Vite конфигурация
- Автоматический импорт PrimeVue компонентов
- Алиас `@` для папки `src`
- Оптимизация зависимостей

### Tailwind конфигурация
- Интеграция с PrimeVue
- Кастомные брейкпоинты
- Поддержка темного режима

## 📊 API интеграция

Приложение интегрируется с backend API через:
- **Axios** для HTTP запросов
- Централизованная конфигурация API
- Сервисы для различных модулей (ProductService, CustomerService и др.)

## 🚀 Деплой

### GitHub Pages
```bash
npm run deploy
```

### Vercel
Конфигурация для Vercel включена в `vercel.json`

## 📝 Скрипты

- `dev` - Запуск в режиме разработки
- `build` - Сборка для продакшена
- `preview` - Предварительный просмотр сборки
- `lint` - Проверка и исправление кода
- `deploy` - Деплой на GitHub Pages

## 👨‍💻 Автор

**Ivan Miroshnichenko**

## 📄 Лицензия

Проект распространяется под лицензией, указанной в файле LICENSE.md

## 🔄 Версия

Текущая версия: **1.3.0**